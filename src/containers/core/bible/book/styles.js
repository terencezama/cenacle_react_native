import {StyleSheet} from 'react-native'
const styles = StyleSheet.create({
    closeIcon:{
        alignItems: 'flex-end',
    },
    itemContainer:{
        margin:16
    }
});

export default styles;